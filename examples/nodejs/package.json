{
  "name": "ethers-override-nodejs-examples",
  "version": "1.0.0",
  "main": "index.js",
  "author": "Zheng Wang",
  "license": "MIT",
  "dependencies": {
    "ts-node": "^10.9.2",
    "ethers-override": "file:../.."
  },
  "devDependencies": {
    "@babel/cli": "^7.12.16",
    "@babel/core": "^7.12.16",
    "@babel/plugin-proposal-class-properties": "^7.16.7",
    "@babel/plugin-proposal-decorators": "^7.17.2",
    "@babel/preset-env": "^7.12.16",
    "@babel/preset-typescript": "^7.12.16",
    "@types/node": "^18.11.18",
    "typescript": "^4.9.4",
    "watch": "^1.0.2"
  },
  "scripts": {
    "build": "tsc && babel src --out-dir dist --extensions .ts",
    "start": "node index.js",
    "start:benchmark": "yarn build && node -r dotenv/config dist/benchmark/runner.js",
    "start:config": "yarn build && node -r dotenv/config dist/tokenFinder.js",
    "start:trade": "yarn build && node -r dotenv/config dist/examples/trade.js",
    "watch": "watch --interval=2 'yarn start' ./src",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}